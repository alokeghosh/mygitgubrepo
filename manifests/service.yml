apiVersion: v1
kind: Service
metadata:
    name: "myakscluster-2515"
    labels:
        app: "myakscluster-2515"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myakscluster-2515"